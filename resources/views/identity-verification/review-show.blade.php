@extends('layout', ['title' => 'Butiran Semakan Identiti'])
@section('content')
<div class="page-head"><div><p class="eyebrow">{{ __('ui.semakan_manual.51918cc4') }}</p><h2>{{ $review->session->user->name }}</h2><p>ID sesi: {{ $review->session_id }}</p></div><a class="btn" href="{{ route('identity-reviews.index') }}">{{ __('ui.kembali.c43a6e25') }}</a></div>
<div class="dashboard-grid">
<section class="panel" style="padding:20px"><h3>{{ __('ui.dokumen.a809e950') }}</h3><div class="job-grid">@foreach($review->session->documents as $document)<article><strong>{{ str_replace('_',' ',strtoupper($document->document_type)) }}</strong><p>Skor kualiti: {{ number_format($document->quality_score * 100) }}%</p><a class="btn" target="_blank" rel="noopener" href="{{ route('identity-reviews.document', [$review, $document]) }}">{{ __('ui.lihat_imej.3e996cf9') }}</a></article>@endforeach</div></section>
<aside class="panel" style="padding:20px"><h3>{{ __('ui.keputusan.63dcf2fe') }}</h3><p>Sebab: {{ implode(', ', $review->reason_codes) }}</p><p>{{ __('ui.nric_hanya_dipaparkan_dalam_bentuk_bertopeng_pada.9a10c590') }}</p><form method="post" action="{{ route('identity-reviews.update', $review) }}">@csrf @method('PUT')<div class="form-group"><label for="status">{{ __('ui.keputusan_pegawai.a6ba55ff') }}</label><select class="select" id="status" name="status" required>@foreach(['APPROVED','REJECTED','NEEDS_RESUBMISSION'] as $status)<option>{{ $status }}</option>@endforeach</select></div><button class="btn btn-primary" type="submit">{{ __('ui.simpan_keputusan.94bc7836') }}</button></form></aside>
</div>
@endsection
