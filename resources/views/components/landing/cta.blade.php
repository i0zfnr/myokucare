<section class="welcome-cta reveal" aria-labelledby="welcome-cta-heading">
    <div>
        <h2 id="welcome-cta-heading">{{ __('ui.sedia_untuk_menggunakan_myokucare.233a2473') }}</h2>
        <p>{{ __('ui.daftar_akaun_baharu_atau_log_masuk_menggunakan.1b5a06fa') }}</p>
    </div>
    <div class="welcome-cta-actions">
        <a class="btn btn-ripple" href="{{ route('register') }}">{{ __('ui.daftar_sekarang.a11b91f7') }}</a>
        <a class="btn cta-login btn-ripple btn-ripple-outline" href="{{ route('login') }}">{{ __('ui.log_masuk.65586411') }}</a>
    </div>
</section>
